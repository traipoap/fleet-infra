apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-exports
  namespace: lumina
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Block
  resources:
    requests:
      storage: 8Gi
