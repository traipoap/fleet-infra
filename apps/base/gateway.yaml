apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: app-gateway
  namespace: lumina
  annotations:
    cert-manager.io/cluster-issuer: "cluster-ca"
    gateway.istio.io/proxy-cpu-limit: "10m"
    gateway.istio.io/proxy-cpu-request: "5m"
    gateway.istio.io/proxy-memory-limit: "100Mi"
    gateway.istio.io/proxy-memory-request: "50Mi"
spec:
  gatewayClassName: istio
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    hostname: "app.codezap.win"
    allowedRoutes:
      namespaces:
        from: "All"
  - name: https
    protocol: HTTPS
    port: 443
    hostname: "app.codezap.win"
    tls:
      mode: Terminate
      certificateRefs:
      - name: app-codezap-win-tls
        kind: Secret
    allowedRoutes:
      namespaces:
        from: All
