apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# cert-manager CRDs + controller are installed via HelmRelease (sources/cert-manager-repo.yaml)
# ClusterIssuers are applied by Flux Kustomization 'cert-manager-config' with dependsOn
resources: []
