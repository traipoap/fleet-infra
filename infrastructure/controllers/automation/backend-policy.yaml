---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: backend
  namespace: lumina
spec:
  imageRepositoryRef:
    name: backend
    namespace: lumina
  policy:
    semver:
      range: ">=0.0.23 <0.0.40"
