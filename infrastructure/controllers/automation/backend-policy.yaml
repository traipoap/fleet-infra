---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: backend
  namespace: lumina
spec:
  imageRepositoryRef:
    name: backend
    namespace: flux-system
  policy:
    semver:
      range: ">=0.0.0"
