---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: frontend
  namespace: lumina
spec:
  imageRepositoryRef:
    name: frontend
    namespace: flux-system
  policy:
    semver:
      range: ">=0.0.0"
